# `Views/CommandPaletteWindow.axaml.cs`

**Purpose:** Command palette code-behind: Enter accepts, Escape closes, Up/Down move the selection, and the window resolves a Task so the query window awaits the choice rather than polling the popup.

**Namespace:** `SchemaCompare.Views`

**Code-behind for:** `Views/CommandPaletteWindow.axaml`

## Declared types

- `class CommandPaletteWindow` : Window — The command palette: type a few letters of a table, view, procedure or command, press Enter.

## Public surface

`SearchText`

## Referenced by

- `Views/CommandPaletteWindow.axaml`
- `Views/QueryWindow.axaml.cs`

**Size:** 85 lines
