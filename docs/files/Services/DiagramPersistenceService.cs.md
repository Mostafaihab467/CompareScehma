# `Services/DiagramPersistenceService.cs`

**Purpose:** Saves and loads diagram layouts as JSON.

**Namespace:** `SchemaCompare.Services`

## Declared types

- `class DiagramPersistenceService` — Persists diagram layouts (node positions, visibility, zoom) as JSON.

## Public surface

`DiagramPersistenceService`, `GetAutoSavePath()`, `Save()`, `TryLoad()`, `SaveToFile()`, `LoadFromFile()`, `ApplyToTables()`

## Referenced by

- `ViewModels/DiagramViewModel.cs`

**Size:** 80 lines
