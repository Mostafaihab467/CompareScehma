# `Services/TsqlHighlighting.cs`

**Purpose:** Loads the bundled TSQL.xshd into the editor's highlighting definitions.

**Namespace:** `SchemaCompare.Services`

## Declared types

- `class TsqlHighlighting` — Loads the bundled T-SQL highlighting definition (VS-style colors).

## Public surface

`TsqlHighlighting`, `Get()`, `Apply()`

## Referenced by

- `Controls/SqlHighlightedEditor.cs`

**Size:** 69 lines
