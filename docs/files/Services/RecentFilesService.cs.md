# `Services/RecentFilesService.cs`

**Purpose:** Most-recently-opened .sql paths, stored without file contents.

**Namespace:** `SchemaCompare.Services`

## Declared types

- `class RecentFilesService` — Most recently opened or saved .sql files, persisted as plain paths (no file contents) in the data folder.

## Public surface

`RecentFilesService`, `Paths`, `Load()`, `Touch()`

## Referenced by

- `ViewModels/QueryViewModel.cs`

**Size:** 59 lines
