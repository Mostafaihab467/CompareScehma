# `Services/LineDiffer.cs`

**Purpose:** Line-level LCS diff used to colour source-only and target-only lines.

**Namespace:** `SchemaCompare.Services`

## Declared types

- `class LineDiffer` — Line-level LCS so schema compare can paint source-only lines green and target-only lines red (the same visual language as SSMS / git diffs).

## Public surface

`LineDiffer`, `UniqueLines()`

## Referenced by

- `Controls/SqlHighlightedEditor.cs`

**Size:** 82 lines
