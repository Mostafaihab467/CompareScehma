# `Models/SnapshotEntry.cs`

**Purpose:** One remembered baseline as the list keeps it: the path plus the provenance copied out of the package, and the single line a row shows.

**Namespace:** `SchemaCompare.Models`

## Declared types

- `class SnapshotEntry` — One snapshot file the operator has captured or used, as the library remembers it.

## Public surface

`SnapshotEntry`, `Path`, `CapturedServer`, `CapturedDatabase`, `CapturedAt`, `FileSizeBytes`, `From()`, `SameProvenanceAs()`, `Display`

## Referenced by

- `Models/SnapshotInfo.cs`
- `Services/SnapshotLibraryService.cs`
- `ViewModels/MainViewModel.cs`
- `Views/MainWindow.axaml`

**Size:** 52 lines
