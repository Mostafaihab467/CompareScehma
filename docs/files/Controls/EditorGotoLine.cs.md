# `Controls/EditorGotoLine.cs`

**Purpose:** Ctrl+G go-to-line overlay: clamps past the end, rejects non-numbers, Escape to close.

**Namespace:** `SchemaCompare.Controls`

## Declared types

- `class EditorGotoLine` — Ctrl+G go-to-line overlay for a AvaloniaEdit , hosted by the same way is.

## Public surface

`EditorGotoLine`, `HandleKeyDown()`, `Open()`, `Close()`, `TryJump()`, `GoToLine()`

## Referenced by

- `Controls/SqlHighlightedEditor.cs`

**Size:** 128 lines
